query GetEpisodes($page: Int) {
  episodes(page: $page) {
  info {
  			pages, count, next, prev
  		}
    results {
      name
      episode,
      id,
      characters {
        id,
        name,
        image
      }
    }
  }
}
